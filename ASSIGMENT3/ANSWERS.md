##
<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

</Project>
##2 
No region and endregion is used for formatting code 

##3 xml documentation when want to document a class, method, property
, parameter, or return value, especially when other developers will use your code.


##4
C# doesn't have true global variables. A static field/property inside a class is the closest equivalent.